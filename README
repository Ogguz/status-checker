A simple go app that checks urls(simple http get request) and if it can not reach it, warns via email.

By default, after every (per url) alive check request, it sleeps for 5 seconds. You can change it from config file.

Multiple receivers supported.

Usage:

$ go run main.go send_email.go
